You will be given a product description. Draft a brand profile from it.

Do not invent fields you do not know. Leave anything uncertain empty. In particular, put only what the text explicitly states into the proof field.
